{
  "name": "astroid-canvas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/game.mjs --bundle --outfile=public/game.mjs --format=esm --platform=neutral",
    "start": "esbuild src/game.mjs --bundle --outfile=public/game.mjs --watch --servedir=public --format=esm --platform=neutral"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.18.1"
  }
}
